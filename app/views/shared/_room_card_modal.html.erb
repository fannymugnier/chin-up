<div class="room-modal room-modal-<%= room.id %> modal fade" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-room-card modal-dialog-centered" role="document">
    <div class="modal-content modal-content-room-card">
      <div class="modal-header p-0 mb-2">
        <h2 class="modal-title" id="exampleModalLongTitle"><%= room.name %></h2>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body p-0">
        <p class="font-italic"><%= room.description %></p>
        <h4 class="mt-4 mb-3">Actuellement dans la room</h4>
        <% if room.messages.where(message_type: "topic").last %>
          <p class="live-topic"><span class="blinking">🔴</span> Sujet en cours : <%= room.messages.where(message_type: "topic").last.content %></p>
        <% else %>
          <p class="live-topic"><span class="blinking">🔴</span> Aucun sujet en cours.</p>
        <% end %>
        <p class="mt-3"><i class="far fa-grin"></i></i> <%= pluralize(room.online_users.count, "chinner")%></p>
      </div>
      <div class="modal-footer p-0">
          <%= link_to room_path(room), target: "_blank" do %>
            <button type="button" class="btn btn-main">Rejoindre</button>
          <% end %>
        <button class="btn btn-main-no-border" type="button" data-dismiss="modal">Annuler</button>
      </div>
    </div>
  </div>
</div>
